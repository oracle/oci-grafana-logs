# Repository Instructions

- Always use `https://registry.yarnpkg.com` for Yarn dependencies. Never write Oracle-internal registry URLs to `yarn.lock`.
- Always use `https://proxy.golang.org,direct` for public Go modules. Never require an Oracle-internal Go proxy.
